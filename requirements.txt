# Dependencies for the Streamlit app (streamlit_app.py).
# Streamlit Community Cloud reads this file from the repo root.
#
# The app runs potato_model.tflite through the LiteRT interpreter instead of
# loading the Keras SavedModel, so TensorFlow is not needed here. That matters:
# tensorflow 2.15 is the last release able to read this repo's 2021 Keras 2
# exports, and it has no wheels past Python 3.11, which made deployment
# depend on pinning the runtime exactly right.
streamlit
ai-edge-litert
numpy
pillow
